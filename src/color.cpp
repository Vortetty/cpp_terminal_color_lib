#include "color.hpp"
#include <stdint.h>
#include <string>

std::string colors::getColor(colors::cl cl, colors::termColor8 color) {

}
std::string colors::getColor(colors::colorLayer colorLayer, colors::termColor8 color);

std::string colors::getColor(colors::cl cl, colors::termColor24 color);
std::string colors::getColor(colors::colorLayer cl, colors::termColor24 color);